defmodule TestExpandTest do
  use ExUnit.Case
  doctest TestExpand

  test "greets the world" do
    assert TestExpand.hello() == [
             :at,
             :bag_distance,
             :capitalize,
             :capitalize,
             :chunk,
             :codepoints,
             :contains?,
             :downcase,
             :downcase,
             :duplicate,
             :ends_with?,
             :equivalent?,
             :first,
             :graphemes,
             :jaro_distance,
             :last,
             :length,
             :ljust,
             :ljust,
             :lstrip,
             :lstrip,
             :match?,
             :myers_difference,
             :next_codepoint,
             :next_grapheme,
             :next_grapheme_size,
             :normalize,
             :pad_leading,
             :pad_leading,
             :pad_trailing,
             :pad_trailing,
             :printable?,
             :printable?,
             :replace,
             :replace,
             :replace_leading,
             :replace_prefix,
             :replace_suffix,
             :replace_trailing,
             :reverse,
             :rjust,
             :rjust,
             :rstrip,
             :rstrip,
             :slice,
             :slice,
             :split,
             :split,
             :split,
             :split_at,
             :splitter,
             :splitter,
             :starts_with?,
             :strip,
             :strip,
             :to_atom,
             :to_char_list,
             :to_charlist,
             :to_existing_atom,
             :to_float,
             :to_integer,
             :to_integer,
             :trim,
             :trim,
             :trim_leading,
             :trim_leading,
             :trim_trailing,
             :trim_trailing,
             :upcase,
             :upcase,
             :valid?,
             :valid_character?
           ]
  end
end
